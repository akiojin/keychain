name: Build

on: push

jobs:
  Build:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: 🌱 Checkout to the branch
        uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm run build
